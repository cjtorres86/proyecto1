NODE_ENV=development
PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=gdp_app
DB_PASSWORD=CAMBIAR_EN_PRODUCCION
DB_DATABASE=gdp_slep

JWT_SECRET=CAMBIAR_EN_PRODUCCION
JWT_EXPIRES_IN=8h

# URL donde corre el frontend — Puppeteer navega ahí para generar el PDF
# del informe (PdfService). En producción, cambiar a la URL real.
FRONTEND_URL=http://localhost:4200
